services:
  php:
    image: ${IMAGES_PREFIX:-}app-php
    restart: unless-stopped
    environment:
      SERVER_NAME: ${SERVER_NAME:-localhost} #, php:80
      SYMFONY_VERSION: ${SYMFONY_VERSION:-}
      STABILITY: ${STABILITY:-stable}
      MERCURE_PUBLISHER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      MERCURE_SUBSCRIBER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      # Run "composer require symfony/orm-pack" to install and configure Doctrine ORM
      DATABASE_URL: postgresql://${POSTGRES_USER:-app}:${POSTGRES_PASSWORD:-!ChangeMe!}@database:5432/${POSTGRES_DB:-app}?serverVersion=${POSTGRES_VERSION:-15}&charset=${POSTGRES_CHARSET:-utf8}
      RUN_MIGRATIONS: true
      # Run "composer require symfony/mercure-bundle" to install and configure the Mercure integration
      MESSENGER_TRANSPORT_DSN: ${MESSENGER_TRANSPORT_DSN:-amqp://guest:guest@rabbitmq:5672/%2f}
      # REDIS_HOST: redis
      # REDIS_PORT: 6379
      MERCURE_URL: ${CADDY_MERCURE_URL:-https://symfony.localhost:443/.well-known/mercure}
      MERCURE_PUBLIC_URL: ${CADDY_MERCURE_PUBLIC_URL:-https://${SERVER_NAME:-symfony.localhost}:${HTTPS_PORT:-443}/.well-known/mercure}
      MERCURE_JWT_SECRET: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      # The two next lines can be removed after initial installation
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - redis
    ports:
      # HTTP
      - target: 80
        published: ${HTTP_PORT:-80}
        protocol: tcp
      # HTTPS
      - target: 443
        published: ${HTTPS_PORT:-443}
        protocol: tcp
      # HTTP/3
      - target: 443
        published: ${HTTP3_PORT:-443}
        protocol: udp

# Mercure is installed as a Caddy module, prevent the Flex recipe from installing another service
###> symfony/mercure-bundle ###
###< symfony/mercure-bundle ###

###> doctrine/doctrine-bundle ###
  database:
    image: postgres:${POSTGRES_VERSION:-16}-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-app}
      # You should definitely change the password in production
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}
      POSTGRES_USER: ${POSTGRES_USER:-app}
    healthcheck:
      test: ["CMD", "pg_isready", "-d", "${POSTGRES_DB:-app}", "-U", "${POSTGRES_USER:-app}"]
      timeout: 5s
      retries: 5
      start_period: 60s
    volumes:
      - database_data:/var/lib/postgresql/data:rw
      # You may use a bind-mounted host directory instead, so that it is harder to accidentally remove the volume and lose all your data!
      # - ./docker/db/data:/var/lib/postgresql/data:rw
###< doctrine/doctrine-bundle ###

  rabbitmq:
    image: heidiks/rabbitmq-delayed-message-exchange:latest
    ports:
      - "5672:5672" # AMQP port
      - "15672:15672" # Management UI port
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck: # Docker's healthcheck for the RabbitMQ service
      test: ["CMD", "rabbitmq-diagnostics", "status"] # Checks RabbitMQ's internal status
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 5s
    # volumes:
    #   - ./rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf

  # RabbitMQ Topology Declaration Service
  rabbitmq_topology:
    image: heidiks/rabbitmq-delayed-message-exchange:latest
    depends_on:
      rabbitmq:
        condition: service_healthy
    volumes:
      - ./rabbitmq/rabbitmq_topology.sh:/usr/local/bin/rabbitmq_topology.sh:ro
    command: /usr/local/bin/rabbitmq_topology.sh
  
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    # volumes:
    #   - redis_data:/data

  worker_submission_normal:
    scale: 2
    build:
      context: ./worker
      dockerfile: Dockerfile
    command: >
      celery -A app.celery worker
      -Q submission_normal_queue
      -l info
      --concurrency=2
      --prefetch-multiplier=1
    # environment:
    #   RABBITMQ_HOST: rabbitmq
    #   REDIS_HOST: redis
    #   POSTGRES_HOST: database
    #   POSTGRES_DB: ${POSTGRES_DB:-app}
    #   POSTGRES_USER: ${POSTGRES_USER:-app}
    #   POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}
    depends_on:
      - rabbitmq
      - database
      - redis
      - rabbitmq_topology
    volumes:
      - ./worker:/usr/src

  worker_submission_premium:
    build:
      context: ./worker
      dockerfile: Dockerfile
    command: >
      celery -A app.celery worker
      -Q submission_premium_queue
      -l info
      --concurrency=1
      --prefetch-multiplier=1
    # environment:
    #   RABBITMQ_HOST: rabbitmq
    #   REDIS_HOST: redis
    #   POSTGRES_HOST: database
    #   POSTGRES_DB: ${POSTGRES_DB:-app}
    #   POSTGRES_USER: ${POSTGRES_USER:-app}
    #   POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}
    depends_on:
      - rabbitmq
      - database
      - redis
      - rabbitmq_topology
    volumes:
      - ./worker:/usr/src

  # worker_celery:
  #   build:
  #     context: ./worker
  #     dockerfile: Dockerfile

  # worker_competition_winner_generation:
  #   build:
  #     context: ./worker
  #     dockerfile: Dockerfile
  #   command: >
  #     celery -A app.celery worker
  #     -Q competition_winner_generation_queue
  #     -l info
  #     --concurrency=1
  #     --prefetch-multiplier=1
  #   # environment:
  #   #   RABBITMQ_HOST: rabbitmq
  #   #   REDIS_HOST: redis
  #   #   POSTGRES_HOST: database
  #   #   POSTGRES_DB: ${POSTGRES_DB:-app}
  #   #   POSTGRES_USER: ${POSTGRES_USER:-app}
  #   #   POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-!ChangeMe!}
  #   depends_on:
  #     - rabbitmq
  #     - database
  #     - redis
  #     - rabbitmq_topology
  #   volumes:
  #     - ./worker:/usr/src

  worker_competition_winner_generation:
    image: ${IMAGES_PREFIX:-}app-php-worker
    build:
      context: .
      target: frankenphp_dev
    restart: unless-stopped
    environment:
      SERVER_NAME: ${SERVER_NAME:-localhost} #, php:80 
      MERCURE_PUBLISHER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      MERCURE_SUBSCRIBER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      DATABASE_URL: postgresql://${POSTGRES_USER:-app}:${POSTGRES_PASSWORD:-!ChangeMe!}@database:5432/${POSTGRES_DB:-app}?serverVersion=${POSTGRES_VERSION:-15}&charset=${POSTGRES_CHARSET:-utf8}
      RUN_MIGRATIONS: false
      MESSENGER_TRANSPORT_DSN: ${MESSENGER_TRANSPORT_DSN:-amqp://guest:guest@rabbitmq:5672/%2f}
      MERCURE_URL: ${CADDY_MERCURE_URL:-https://symfony.localhost:443/.well-known/mercure}
      MERCURE_PUBLIC_URL: ${CADDY_MERCURE_PUBLIC_URL:-https://${SERVER_NAME:-symfony.localhost}:${HTTPS_PORT:-443}/.well-known/mercure}
      MERCURE_JWT_SECRET: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
    # working_dir: /srv/app
    volumes:
      - ./:/app
      - /app/var/
    depends_on:
      redis:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      database:
        condition: service_healthy
      php:
        condition: service_started
    command: php bin/console messenger:consume winner_trigger_amqp --verbose --limit=100 --time-limit=3600 --memory-limit=128M
    healthcheck:
      test: ["CMD", "php", "bin/console", "about"]
      interval: 3s
      timeout: 15s
      retries: 3
      start_period: 15s


  worker_email:
    image: ${IMAGES_PREFIX:-}app-php-worker
    build:
      context: .
      target: frankenphp_dev
    restart: unless-stopped
    environment:
      SERVER_NAME: ${SERVER_NAME:-localhost} #, php:80
      MERCURE_PUBLISHER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      MERCURE_SUBSCRIBER_JWT_KEY: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
      DATABASE_URL: postgresql://${POSTGRES_USER:-app}:${POSTGRES_PASSWORD:-!ChangeMe!}@database:5432/${POSTGRES_DB:-app}?serverVersion=${POSTGRES_VERSION:-15}&charset=${POSTGRES_CHARSET:-utf8}
      RUN_MIGRATIONS: false
      MESSENGER_TRANSPORT_DSN: ${MESSENGER_TRANSPORT_DSN:-amqp://guest:guest@rabbitmq:5672/%2f}
      MERCURE_URL: ${CADDY_MERCURE_URL:-https://symfony.localhost:443/.well-known/mercure}
      MERCURE_PUBLIC_URL: ${CADDY_MERCURE_PUBLIC_URL:-https://${SERVER_NAME:-symfony.localhost}:${HTTPS_PORT:-443}/.well-known/mercure}
      MERCURE_JWT_SECRET: ${CADDY_MERCURE_JWT_SECRET:-!ChangeThisMercureHubJWTSecretKey!}
    # working_dir: /srv/app
    volumes:
      - ./:/app
      - /app/var/
    depends_on:
      redis:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      database:
        condition: service_healthy
      php:
        condition: service_started
    command: php bin/console messenger:consume email_amqp --verbose --limit=100 --time-limit=3600 --memory-limit=128M
    healthcheck:
      test: ["CMD", "php", "bin/console", "about"]
      interval: 3s
      timeout: 15s
      retries: 3
      start_period: 15s
  # https://github.com/k-37/dolphin-is-symfony-ddd-experiment/


volumes:
  caddy_data:
  caddy_config:
###> symfony/mercure-bundle ###
###< symfony/mercure-bundle ###

###> doctrine/doctrine-bundle ###
  database_data:
###< doctrine/doctrine-bundle ###
