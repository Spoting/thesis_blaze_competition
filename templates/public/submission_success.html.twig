{% extends 'base.html.twig' %} 

{% block title %}Submission Confirmed!{% endblock %}

{% block body %}
    <div class="container mx-auto mt-10 p-4"> {# Outer container for spacing #}
        {# NEW: Inner container with white background, padding, rounded corners, and shadow #}
        <div class="bg-white p-8 rounded-lg shadow-md max-w-lg mx-auto"> {# max-w-lg to limit width, mx-auto to center #}
            <h1 class="text-4xl font-bold text-green-600 mb-4">Submission Confirmed!</h1>
            <p class="text-lg text-gray-700 mb-4">Thank you for verifying your email. Your entry has been successfully recorded.</p>
            <p class="text-gray-600">You can now close this page.</p>
            <a href="{{ path('public_competitions_list') }}" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Return to Homepage
            </a>
        </div>
    </div>
{% endblock %}