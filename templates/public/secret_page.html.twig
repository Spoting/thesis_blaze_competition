<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A Special Message For You</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts: Dancing Script for a romantic feel -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
    <style>
        /* Applying the custom font */
        .font-dancing {
            font-family: 'Dancing Script', cursive;
        }
        /* Adding a text shadow to make the text pop against the background */
        .text-shadow {
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7);
        }
        /* A fade-in animation for the text */
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }
        .animate-fade-in {
            animation: fadeIn 2.5s ease-out forwards;
        }
    </style>
</head>
<body>

    <!-- 
      Main container with a full-screen background image.
      I've chosen a beautiful image of a starry night sky over a serene landscape.
      You can replace the URL with any other image you prefer.
    -->
    <div 
      class="min-h-screen bg-cover bg-center flex items-center justify-center p-4" 
      style="background-image: url('https://images.unsplash.com/photo-1534796636912-3b95b3ab5986?q=80&w=2071&auto=format&fit=crop')"
      onerror="this.onerror=null;this.style.backgroundImage='url(https://placehold.co/1920x1080/1a2a45/ffffff?text=Imagine+a+beautiful+night+sky...)'"
    >
        
        <!-- Content container with a semi-transparent background for readability -->
        <div class="text-center bg-black/30 backdrop-blur-sm p-8 sm:p-12 rounded-2xl shadow-2xl animate-fade-in">
            <h1 class="font-dancing text-6xl sm:text-8xl font-bold text-white text-shadow">
                I love you
            </h1>
            <h2 class="mt-4 font-dancing text-4xl sm:text-6xl text-white text-shadow">
                Εμείς και Αυτοί
            </h2>
            
        </div>

    </div>

</body>
</html>
