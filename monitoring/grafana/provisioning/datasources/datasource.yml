apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090 # Connects to the Prometheus service within the Docker network
    access: proxy
    isDefault: true
    editable: true